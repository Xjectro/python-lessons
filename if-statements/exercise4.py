username = input("Username: ") # We get the username from the user 
password = input("Password: ") # We get the password from the user 

if username == "emre" and password == "ruhi123":
    print("Login successful") # If the password and username we specify match, we say the login is successful. 
else:
    print("Wrong password") # If the information in the definitions we provide does not match, we say wrong password. 